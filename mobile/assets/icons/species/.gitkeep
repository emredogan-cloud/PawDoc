Species art drops in here — see docs/ASSET_PROMPTS.md for the exact filenames,
sizes and generation prompts. The app picks each file up automatically; until
then every chip and the reduce-motion avatar fall back to its emoji.
