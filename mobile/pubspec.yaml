name: pawdoc
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # Phase 5.4 — i18n / l10n (English + German launch, CR #11). The Edge analyze
  # function passes the user's preferred_locale to the AI service so the same
  # locale value (e.g. 'de') reaches both the emergency-keyword override
  # (safety-critical) and the UI strings here.
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  go_router: ^17.2.3
  supabase_flutter: ^2.12.4
  sign_in_with_apple: ^8.0.0
  sentry_flutter: ^8.14.2
  crypto: ^3.0.7
  camera: ^0.12.0+1
  # Phase 3.2 — client-side video keyframe extraction. Chosen over ffmpeg-based
  # packages: native platform extractors (no GPL/LGPL ffmpeg binaries, far
  # smaller build, actively maintained). See docs / sub-PR report for rationale.
  video_thumbnail: ^0.5.3
  image: ^4.8.0
  posthog_flutter: ^5.24.2
  http: ^1.6.0
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  share_plus: ^13.1.0
  url_launcher: ^6.3.2
  # Phase 3.4 — vet finder location (permissions handled gracefully; the Places
  # API key stays server-side in the /find-vets Edge Function).
  geolocator: ^13.0.0
  purchases_flutter: ^10.1.1
  shared_preferences: ^2.5.5
  onesignal_flutter: ^5.5.5
  connectivity_plus: ^7.1.1
  google_fonts: ^8.1.0
  flutter_animate: ^4.5.2
  animations: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  # M0 F-1 tests — drive the delete-account timeout deterministically.
  fake_async: ^1.3.1

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_native_splash: ^2.4.7
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # Generate AppLocalizations from lib/l10n/*.arb (configured by l10n.yaml).
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # UI redesign asset tree (PAWDOC_UI_UX_MASTER_ROADMAP.md §7.1). Folders ship
  # with a .gitkeep placeholder and are populated as art is produced; every
  # illustration is rendered via AppImage with a code fallback, so a missing
  # asset never breaks the build or the UI.
  assets:
    - assets/brand/
    - assets/icon/
    - assets/illustrations/onboarding/
    - assets/illustrations/analysis/
    - assets/illustrations/empty_states/
    - assets/illustrations/monetization/
    - assets/illustrations/growth/
    - assets/illustrations/system/
    - assets/icons/species/
    - assets/icons/actions/
    - assets/icons/status/
    - assets/icons/avatars/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Fonts (Inter + Bricolage Grotesque; see theme/design_tokens.dart) are loaded
  # at runtime via the `google_fonts` package and cached on device. To make them
  # fully offline-deterministic, drop the .ttf files into assets/fonts/, declare
  # them in a `fonts:` block below, and set
  # GoogleFonts.config.allowRuntimeFetching = false at startup.
  #
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Native splash (Phase 2.1). Color-only is enough to generate now; add a logo
# image later (`image:`) and re-run `dart run flutter_native_splash:create`.
flutter_native_splash:
  color: "#FFFFFF"
  color_dark: "#121212"

# App icons (Phase 2.1). Add the asset(s) then run `dart run flutter_launcher_icons`.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
  adaptive_icon_background: "#00897B"
  adaptive_icon_foreground: "assets/icon/app_icon.png"
