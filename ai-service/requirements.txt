# PawDoc AI service — runtime deps.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
# Phase 1.3 — providers (lazy-imported) + Upstash REST transport.
# GAP-E11: pin the provider/transport deps (were floor-only, no upper bound) so
# prod builds are reproducible. Versions match the validated venv; bump deliberately.
anthropic==0.104.1
google-genai==2.6.0
httpx==0.28.1
# Phase 5.3 — OpenAI (GPT-4o) for the weekly Health Journal. Lazy-imported and
# not in the test venv; bound the major until a deploy pins the exact version.
openai>=1.40,<2.0
# GAP-D2: error monitoring (lazy-imported in main._init_sentry; no-op without a DSN).
sentry-sdk[fastapi]==2.62.0
